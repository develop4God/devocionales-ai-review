"""
reader_langgraph — a simulated native-speaker reader, orchestrated as a LangGraph pipeline.

A new project, not a port. It reuses the concepts proven in GEP_Genome-Evolution-Protocol
(the reader-persona prompting approach, an evolving pattern memory, an audited review trail,
provider-agnostic model routing) but reimplements them here on LangGraph/LangChain rather than
importing GEP's code — this is a fresh package with its own identity and its own tests.
"""
